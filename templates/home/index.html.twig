{% extends 'base.html.twig' %}

{% block title %}Symfony Docker API{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">
        <h1>✅ Welcome to Symfony Docker API Documentation ✅</h1>

        <ul>
            <li>You can find the documentation on :
                <code><a href="{{ docRoute }}">{{ docRoute }}</a></code></li>
            <li> or connect to back office at <code><a
                            href="{{ path('backoffice_bo_index') }}">{{ path('backoffice_bo_index') }}</a></code>
            </li>
            <li>See our <a href="{{ path('changelog') }}">changelog</a></li>
        </ul>
    </div>
{% endblock %}
